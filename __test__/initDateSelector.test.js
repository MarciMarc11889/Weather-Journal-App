import {setDateLimits} from "../src/client/js/initDateSelector"


describe("Testing the submit functionality", () => {
    test("Testing the postData() function", () => {
        expect(setDateLimits).toBeDefined();
    })});