import {checkDate} from "../src/server/index"


describe("Testing the submit functionality", () => {
    test("Testing the action() function", () => {
        expect(checkDate).toBeDefined();
    })});